{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && python download_weights.py && python manage.py migrate --run-syncdb && python manage.py collectstatic --noinput && gunicorn core.wsgi --bind 0.0.0.0:$PORT --timeout 120 --workers 1",
    "healthcheckPath": "/api/health/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
